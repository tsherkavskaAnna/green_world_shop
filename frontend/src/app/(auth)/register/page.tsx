import RegisterForm from '@/components/Forms/Register';

export const metadata = {
  title: 'Dashboard User - Register',
};

const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;
